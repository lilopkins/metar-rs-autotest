[package]
name = "metar-test"
version = "0.6.1"
authors = ["Lily Hopkins <lily@hpkns.uk>"]
edition = "2018"
publish = false

[profile.release]
lto = true
panic = "abort"

[dependencies]
metar = "0.7"
reqwest = { version = "0.11.10", features = ["blocking"] }
regex = "1.4.2"
