[package]
name = "metar-test"
version = "0.1.6"
authors = ["Lily Hopkins <bsalarius@gmail.com>"]
edition = "2018"

[profile.release]
lto = true
panic = "abort"

[dependencies]
metar = ""
reqwest = { version = "0.10", features = ["blocking"] }
regex = "1.3"
