[package]
name = "metar-test"
version = "0.6.0"
authors = ["Lily Hopkins <bsalarius@gmail.com>"]
edition = "2018"

[profile.release]
lto = true
panic = "abort"

[dependencies]
metar = "0.6"
reqwest = { version = "0.11.10", features = ["blocking"] }
regex = "1.4.2"
